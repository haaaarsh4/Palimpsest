# Copy this file to .env and fill in your key to enable the AI Insight panel.
# Without it, the rest of the app (timeline, diffing, notes) still works fine,
# the Insight tab will just say it's not configured.

ANTHROPIC_API_KEY=

# Optional, defaults to 3000
PORT=3000
